import { Person } from "./person.js";
export class Teacher extends Person {
    constructor(firstName, lastName, document = "") {
        super(firstName, lastName);
        this.document = document;
        this.document = document;
    }
}
